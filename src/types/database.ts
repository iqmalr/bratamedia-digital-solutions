/**
 * Supabase database type definitions.
 *
 * This file is a placeholder. Once the database schema is defined, replace
 * this with the output of the Supabase CLI type generator:
 *
 *   npx supabase gen types typescript --project-id <project-id> > src/types/database.ts
 *
 * Until then, the `Database` type is set to `unknown` so it propagates
 * through the client helpers without breaking TypeScript strict mode.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type Database = {}
